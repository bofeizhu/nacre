# FalkorDB for golden-trace capture runs (manual, local only).
#
# Digest pinned at the first successful trace1 capture (2026-07-10,
# redis_version 8.6.3). Re-pin deliberately, together with a re-capture.
services:
  falkordb:
    image: falkordb/falkordb@sha256:2496643cabd67e87fd82458383400c049324daec1fe674ba0db4c5bdaca5d25f
    ports:
      - "6380:6379"  # host 6380: another project's Redis owns 6379
